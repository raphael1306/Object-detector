# Object-detector

Python Version:

           Python 3.9.12
           
Packages used:

           pip install VideoCapture
           pip install opencv-python
           
to run the code:

         install the packages and type python main.py
